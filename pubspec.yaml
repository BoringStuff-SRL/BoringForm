name: boring_form
description: A new Flutter package project.
version: 0.0.1

publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.20.1
  provider: ^6.1.2
  file_picker: ^8.1.7
  universal_html: ^2.2.4
  flutter_svg: ^2.0.17
  omni_datetime_picker: ^2.0.5

  boringcore:
    git:
      url: git@github.com:BoringStuff-SRL/BoringCore.git
      ref: main

  desktop_drop:
    git:
      url: git@github.com:BoringStuff-SRL/desktop_drop.git
      ref: main

  widget_zoom: ^0.0.4

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/
